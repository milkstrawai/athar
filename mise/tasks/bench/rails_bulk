#!/usr/bin/env bash
#MISE description="Run the Rails bulk deletion benchmark"

set -euo pipefail

source "$(dirname "$0")/../../support/postgres.sh"

mise run deps:ruby

with_postgres
ensure_database athar_rails_bench

ATHAR_DB_NAME=athar_rails_bench bundle exec ruby bench/rails_bulk_delete_bench.rb
