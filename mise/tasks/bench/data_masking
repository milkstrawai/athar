#!/usr/bin/env bash
#MISE description="Benchmark mask overhead (identity vs snapshot vs masked)"

set -euo pipefail

source "$(dirname "$0")/../../support/postgres.sh"

mise run deps:ruby

with_postgres
ensure_database athar_mask_bench

ATHAR_DB_NAME=athar_mask_bench bundle exec ruby bench/data_masking_bench.rb
