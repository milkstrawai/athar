#!/usr/bin/env bash
#MISE description="Run the dashboard query benchmark against ~1M seeded rows"

set -euo pipefail

source "$(dirname "$0")/../../support/postgres.sh"

mise run deps:ruby

with_postgres
ensure_database athar_dashboard_bench

ATHAR_DB_NAME=athar_dashboard_bench bundle exec ruby bench/dashboard_bench.rb
